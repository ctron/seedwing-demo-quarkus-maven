name: Seedwing Dependency Enforcer

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  enforcer:
    runs-on: ubuntu-22.04
    steps:
      - name: Check dependency tree against provided policies
        uses: seedwing-io/enforcer-action@9786e4f079c6a386fd455d4288db6c2a42b79cb0
        with:
          policy-dir: enforcer.dog
          enforcer-config: .enforcer.yaml
          source: pom.xml
